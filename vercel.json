{
  "version": 2,
  "buildCommand": "node -e \"const fs=require('fs'); const cp=require('child_process'); if (fs.existsSync('frontend/package.json')) { cp.execSync('npm run build --prefix frontend', {stdio:'inherit'}); } else { cp.execSync('npx vite build', {stdio:'inherit'}); }\"",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.js"
    },
    {
      "source": "/uploads/(.*)",
      "destination": "/api/index.js"
    },
    {
      "source": "/((?!api|uploads).*)",
      "destination": "/index.html"
    }
  ]
}
